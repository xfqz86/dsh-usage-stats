# Mounts the usage-statistics card into the profile's plugin tree.
# 存储为自管理 sqlite（node:sqlite，$DSH_HOME/storages/dsh-usage-statistics/ledger.sqlite），
# 不依赖 harness 的 storage 家族，无需额外的行或后端路由。
- insert:
    - id: usage-statistics
      name: 'dsh-usage-statistics'