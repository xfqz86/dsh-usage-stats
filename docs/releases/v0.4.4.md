## 这版多了一个会话 ID 徽标，并修了发布包安装缺模块的问题

- **会话标题旁能看到会话 ID 了**：每个会话标题右侧多了一个等宽小徽标，显示完整会话 ID；鼠标移上去（或键盘聚焦）会浮现复制按钮，点一下就复制，成功后短暂变成对勾。设置页新增「会话」分组开关，默认开启，不想要关掉即可。
- **从 npm 或压缩包安装不再缺模块**：之前从发布包安装时插件可能加载失败，自己开发目录直连不受影响；现在发布包自带完整运行库，装完即用。
- 本次无统计口径变化，历史数据不受影响。

## 升级提醒

更新版本后请重启一次 dsh，再刷新浏览器页面。
