## 这一版修两个会让数据或界面「不对劲」的问题

- **新版 dsh 下用量详情打不开**：dsh 新版换了插件的校验方式，旧包在新版里点开用量详情是打不开的；这一版两边都顾上了，dsh 新旧版本都能正常打开。
- **清零后重启，历史统计又回来了**：此前「清零」只在本次运行里管用，重启 dsh 后插件会重新读一遍历史会话日志，刚清掉的数据原样回来。现在清零后重启也保持归零，清零之后的新会话照常统计；哪天想找回历史数据，用「重建」随时能重新统计出来。

## 升级提醒

更新版本后请重启一次 dsh，再刷新浏览器页面。
