## 日期趋势能看缓存命中率了

日期页的柱状图上叠加一条缓存命中率折线，口径与表格里的命中率一致，一眼看出缓存省了多少；柱子仍按输入 / 输出 / 缓存三段堆叠。

## Z.ai 未开通有了明确的空态

没开通 GLM Coding Plan 时，额度卡不再是一块空面板，而是显示「未开通」的说明，同样支持手动刷新。

## 其他调整

- 日期页与模型页的默认时间范围由「全部」改为最近 1 年。
- 额度卡片排版优化：折叠栏标签更短，「更新于」时间固定在卡片底部。
- 零用量的调用不再计入统计。

## 升级提醒

这次统计口径有调整。更新版本后请重启一次 dsh，再刷新浏览器页面。
