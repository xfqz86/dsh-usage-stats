## 这一版修一个「面板打不开」的问题

- **新版 dsh 上用量面板打不开、额度也不显示**：在新版 dsh 上，侧边栏的今日用量、额度行和详情面板一直停在「数据不可用，重试中」，等多久都不出数。原因是发布包在打包时把服务端方法的参数名改短了，新版 dsh 按这个名字取数据、两边对不上。现在打包保留原参数名，dsh 新旧版本都能正常显示。
- 本次只有修复，没有新功能。

## 升级提醒

更新版本后请重启一次 dsh，再刷新浏览器页面。
